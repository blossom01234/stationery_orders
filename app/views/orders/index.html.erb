<div class="container mt-3">
    <div class="d-flex justify-content-center mb-3">
        <h1>受注一覧</h1>
    </div>
    <div class="table-responsive">
        <%= will_paginate @orders, class: "pagination" %>
        <table class="table table-hover">
            <thead>
                <tr class="">
                    <th>注文ID</th>
                    <th>ステータス</th>
                    <th>合計金額</th>
                    <th>更新日</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% @orders.each do |order| %>
                <tr onClick="test(<%= order.id %>)">
                    <td><%= order.id %></td>
                    <td>data2</td>
                    <td>data3</td>
                    <td>data4</td>
                    <td>data1</td>
                    <td>data1</td>
                </tr>
                <% end %>
            </tbody>
        </table>
    </div>
    <%= will_paginate @orders, class: "pagination" %>
</div>
<script>
    // TODO 注文詳細画面に遷移させる
    function test(val) { Turbo.visit("/") }
</script>